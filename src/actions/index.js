import { intialcatalog, productDetails } from "./catalog-action";
import { addToCompare, removeFromCompare } from "./compare-action";

var actions = { intialcatalog, productDetails, addToCompare, removeFromCompare };

export default actions;